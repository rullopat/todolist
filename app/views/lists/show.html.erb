<div>
    <h1 class="page-title"><%= @list.name %></h1>

    <ul class="p-1 my-2">
    <% @list.tasks.each do |task| %>
        <li><%= task.name %></li>
    <% end %>
    </ul>

    <%= link_to "New task", new_list_task_path(@list), class: "btn-primary" %>

    <%= render 'lists_link' %>
</div>